// @BLOCK:START(Full Artifact)
{
    "compilerOptions": {
        "module": "commonjs",
        "target": "es2020",
        // Set to true to enable basic type checking in your App and Script files.
        "checkJs": false 
    },
    "include": [
        // This line is crucial. It tells VS Code to include all .js files in this
        // directory and its subdirectories, AND to load the type definitions
        // from bridge.d.ts to provide intellisense for the global `bridge` object.
        "./**/*.js",
        "./bridge.d.ts"
    ],
    "exclude": [
        "node_modules"
    ]
}
// @BLOCK:END(Full Artifact)